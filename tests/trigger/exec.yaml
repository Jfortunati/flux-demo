apiVersion: testworkflows.testkube.io/v1
kind: TestWorkflowExecution
metadata:
  name: podinfo-health-exec
  namespace: testkube        # your Testkube agent namespace
  labels:
    trigger: 'flux-crd'
  annotations:
    bump-number: "2"   # bump this string per run (or substitute it)
spec:
  testWorkflow:
    name: podinfo-health     # must match an existing Test Workflow

